---
phase: 01-foundation-fixes-hero-decision
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false
requirements: [FIX-01, FIX-02, FIX-03]
must_haves:
  truths:
    - "Murilo personally clicked 'Baixar CV' in a real browser and the real PDF arrived"
    - "Murilo personally clicked a LinkedIn link and his actual profile loaded"
    - "Murilo personally looked at the exported hero portrait and approved the crop and the full-colour treatment"
    - "The home page and the contact page still render without errors after the fixes"
    - "Phase 2 is unblocked with no unverified assumption underneath it"
  artifacts:
    - path: ".planning/phases/01-foundation-fixes-hero-decision/01-03-SUMMARY.md"
      provides: "Recorded human verification evidence for ROADMAP Phase 1 success criteria 1, 2 and 3"
      contains: "Baixar CV"
  key_links:
    - from: "http://localhost:8080/"
      to: "public/cv/CV MURILO ORTEGA 2026.pdf"
      via: "hero download anchor served by the vite dev server"
      pattern: "cv/CV%20MURILO%20ORTEGA%202026"
    - from: "http://localhost:8080/"
      to: "https://www.linkedin.com/in/murilo-ortega"
      via: "footer social link"
      pattern: "www.linkedin.com/in/murilo-ortega"
---

<objective>
Prove the Phase 1 fixes actually work for a human, in a browser — not just in a diff.

ROADMAP Phase 1 success criterion 1 is explicit that the CV download must be "verified by an actual
manual click-test, not just a code review", and criterion 3 requires the hero photo decision to be
made and documented. Plans 01-01 and 01-02 can both pass every grep they define while the page still
fails to render, the PDF still fails to serve, the LinkedIn profile still 404s, or the crop still cuts
off the top of Murilo's head. Those are exactly the failure modes a string-level assertion cannot
catch — and exactly the failures that would otherwise surface in Phase 2, after the CTA hierarchy has
already been built on top of them. That is the Pitfall 3/4 scenario this whole phase exists to prevent.

This plan is the closing gate on Phase 1: a machine-run end-to-end audit against a live dev server,
followed by a blocking human check.

Purpose: Close ROADMAP Phase 1 success criteria 1, 2 and 3 with real evidence.
Output: Verification evidence recorded in the summary. No source files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-fixes-hero-decision/01-CONTEXT.md
@.planning/phases/01-foundation-fixes-hero-decision/01-01-SUMMARY.md
@.planning/phases/01-foundation-fixes-hero-decision/01-02-SUMMARY.md
@.planning/phases/01-foundation-fixes-hero-decision/01-HERO-PHOTO.md

<ground_truth>
Dev server: `npm run dev` (or `bun dev`) runs `vite dev` on port 8080, host `::`.
Reachable at http://localhost:8080/

The Cloudflare Workers plugin is deliberately skipped in dev (no local `workerd`), so dev runs on
Node/Vite's own server. Everything under `public/` is served from the site root: `public/cv/...`
resolves at `/cv/...`, and `public/assets/home/hero/...` resolves at `/assets/home/hero/...`. That
makes the dev server a valid place to prove the static-asset paths, even though it is not a
Workers-accurate runtime.

Pages to check:
  http://localhost:8080/          home — the CV download anchor and the footer LinkedIn link
  http://localhost:8080/contato   contact page — the second LinkedIn link

Expected once 01-01 and 01-02 are done:
  GET /cv/CV%20MURILO%20ORTEGA%202026.pdf     -> 200, application/pdf, about 349 KB
  GET /assets/home/hero/murilo-hero-960.jpg   -> 200, about 99 KB
  GET /assets/home/hero/murilo-hero-960.avif  -> 200, about 31 KB
  GET /cv/curriculo.pdf                       -> NOT 200 (that path was always fictional)
  HTML of /                                   -> contains www.linkedin.com/in/murilo-ortega in the ld+json
</ground_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the end-to-end audit against a live dev server</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/01-foundation-fixes-hero-decision/01-01-SUMMARY.md — what the link fixes changed, and the commit SHAs
    - .planning/phases/01-foundation-fixes-hero-decision/01-02-SUMMARY.md — the final crop offset and the real exported file sizes
    - .planning/phases/01-foundation-fixes-hero-decision/01-HERO-PHOTO.md — the decision the human is about to approve
    - package.json — confirm the `dev` script name and that no dependency drifted during this phase
  </read_first>
  <action>
    First, confirm you are on `feature/home-recruiter-redesign` and that `main` still points at the
    commit it pointed at before this phase started (`9cbf2e5`). If anything landed on `main`, stop
    immediately and report — a commit on `main` is a live-site deployment, not a lint failure.

    Start the dev server in the background on port 8080 and wait until it answers.

    Assert with real HTTP requests against the running server:
    - `/cv/CV%20MURILO%20ORTEGA%202026.pdf` returns 200 with an `application/pdf` content type
    - `/cv/curriculo.pdf` does NOT return 200 — proving the old path really was dead and the fix was not cosmetic
    - all six hero variants return 200: `murilo-hero-480.jpg`, `-960.jpg`, `-1440.jpg`, `-480.avif`, `-960.avif`, `-1440.avif` under `/assets/home/hero/`
    - the HTML of `/` contains `https://www.linkedin.com/in/murilo-ortega`
    - the HTML of `/` contains the href `/cv/CV%20MURILO%20ORTEGA%202026.pdf`
    - the HTML of `/contato` contains `https://www.linkedin.com/in/murilo-ortega`
    - `/` and `/contato` both return 200, proving no syntax error was introduced into the JSON-LD or the JSX

    Assert against the working tree:
    - `git status --porcelain package.json bun.lock package-lock.json` is empty
    - `git status --porcelain public/assets/about/photos/ public/cv/` is empty

    Separately, resolve the LinkedIn URL over the network once: request
    `https://www.linkedin.com/in/murilo-ortega` and confirm it does not return 404. LinkedIn commonly
    answers automated requests with a 999 or a login redirect — treat anything other than an outright
    404 as inconclusive-but-not-failing, and hand the real check to the human in Task 2. Spend at most
    one request on this.

    Leave the dev server running for Task 2. Record every status code and every count observed; Task 2
    quotes them back to the user.
  </action>
  <verify>
    <automated>bash -c 'set -e; test "$(git rev-parse --abbrev-ref HEAD)" = "feature/home-recruiter-redesign"; curl -sf -o /dev/null "http://localhost:8080/cv/CV%20MURILO%20ORTEGA%202026.pdf"; for w in 480 960 1440; do for e in jpg avif; do curl -sf -o /dev/null "http://localhost:8080/assets/home/hero/murilo-hero-$w.$e"; done; done; curl -sf "http://localhost:8080/" | grep -q "www.linkedin.com/in/murilo-ortega"; curl -sf "http://localhost:8080/" | grep -q "cv/CV%20MURILO%20ORTEGA%202026"; curl -sf "http://localhost:8080/contato" | grep -q "www.linkedin.com/in/murilo-ortega"; test -z "$(git status --porcelain package.json bun.lock public/cv/ public/assets/about/photos/)"; echo E2E_AUDIT_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` outputs `feature/home-recruiter-redesign`, and `git rev-parse main` still outputs `9cbf2e5`
    - `curl -s -o /dev/null -w '%{http_code}' "http://localhost:8080/cv/CV%20MURILO%20ORTEGA%202026.pdf"` outputs `200`
    - `curl -s -o /dev/null -w '%{content_type}' "http://localhost:8080/cv/CV%20MURILO%20ORTEGA%202026.pdf"` contains `application/pdf`
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/cv/curriculo.pdf` outputs something other than `200`
    - All six `/assets/home/hero/murilo-hero-{480,960,1440}.{jpg,avif}` requests return `200`
    - `curl -s http://localhost:8080/ | grep -c "www.linkedin.com/in/murilo-ortega"` returns a value of at least 2 (the footer link plus the JSON-LD `sameAs`)
    - `curl -s http://localhost:8080/ | grep -c "cv/CV%20MURILO%20ORTEGA%202026"` returns at least 1
    - `curl -s http://localhost:8080/contato | grep -c "www.linkedin.com/in/murilo-ortega"` returns at least 1
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/` and the same for `/contato` both output `200`
    - `git status --porcelain package.json bun.lock package-lock.json public/cv/ public/assets/about/photos/` is empty
    - The dev server is still running when the task ends
  </acceptance_criteria>
  <done>Every Phase 1 fix is confirmed working over real HTTP against a running server, `main` is untouched, and the dev server is live for the human check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the CV download, the LinkedIn profile, and the hero crop</name>
  <files>(no files modified — human verification only)</files>
  <read_first>
    - .planning/ROADMAP.md — Phase 1 success criteria 1, 2 and 3, which this checkpoint closes
    - .planning/phases/01-foundation-fixes-hero-decision/01-HERO-PHOTO.md — the decision record being approved
    - Task 1's recorded status codes and counts — quote the real numbers to the user, do not paraphrase
  </read_first>
  <action>
    Present the checkpoint below to the user and stop. Do not proceed, do not self-approve, and do not
    infer approval from the Task 1 audit passing — Task 1 proves the bytes are served, not that the PDF
    is the right document, that the LinkedIn URL belongs to Murilo, or that the crop is the one he wants.

    Before presenting, substitute the real numbers recorded in Task 1 into the `<what-built>` summary:
    the HTTP status codes, the six asset responses, and the grep counts. Do not present placeholder text.

    Collect a per-item outcome for all five checks plus the answer to the D-07 CV-filename question.
    If the user reports a failure on items 1-3, the owning plan is `01-01-PLAN.md`; if on item 4, it is
    `01-02-PLAN.md` (re-export with an adjusted y-offset in `crop=3376:4220:0:{y}`). Route the fix, then
    re-run this checkpoint. Do not mark Phase 1 complete with an open failure.

    Stop the dev server once the checkpoint resolves.
  </action>
  <what-built>
    Phase 1 fixed three blocking items ahead of the Phase 2 hero rebuild, all on the branch
    `feature/home-recruiter-redesign` — nothing has touched `main`, so the live site is unchanged.

    1. FIX-02 — the LinkedIn URL. Three files carried `https://linkedin.com/in/muriloortega` (missing
       `www.`, missing the hyphen): the footer, the contact page, and the machine-readable `Person`
       structured data on the home page. All three now use `https://www.linkedin.com/in/murilo-ortega`.
    2. FIX-01 — the CV download. The home page "Baixar CV" button pointed at `/cv/curriculo.pdf`, a file
       that has never existed. It now points at the real `public/cv/CV MURILO ORTEGA 2026.pdf`. The PDF
       itself was not renamed or moved.
    3. FIX-03 — the hero photo. `public/assets/about/photos/hero-bg.jpg` (3376x6000, 19.7 MB raw studio
       export) was cropped to a 4:5 portrait and exported to six web-ready files at 480x600, 960x1200 and
       1440x1800 in AVIF and JPEG, in full colour, all under 300 KB. The 19.7 MB original is untouched and
       `/sobre` still renders from it. The decision is written up in `01-HERO-PHOTO.md`.

    Automated audit against the running dev server: [substitute the actual status codes and grep counts from Task 1].
  </what-built>
  <how-to-verify>
    The dev server is already running at http://localhost:8080

    1. CV download (ROADMAP success criterion 1 — the click-test the roadmap explicitly requires)
       - Open http://localhost:8080/
       - Click the "Baixar CV" button in the hero
       - Expected: a PDF named "CV MURILO ORTEGA 2026.pdf" downloads, roughly 349 KB
       - Open the downloaded file and confirm it is your actual 2026 CV, not a blank or corrupted file
       - Report: did it download, and is it the right document?

    2. LinkedIn from the home page (ROADMAP success criterion 2)
       - Still on http://localhost:8080/, scroll to the footer and click "LinkedIn"
       - Expected: a new tab opens at https://www.linkedin.com/in/murilo-ortega showing YOUR profile
       - Report: did your own profile load, or a 404 / someone else's profile?

    3. LinkedIn from the contact page
       - Open http://localhost:8080/contato and click "LinkedIn"
       - Expected: same profile, same result
       - Report: same as above

    4. Hero photo crop and colour (ROADMAP success criterion 3)
       - Open http://localhost:8080/assets/home/hero/murilo-hero-1440.jpg
       - Expected: a 4:5 portrait, in FULL COLOUR (not grayscale, not faded), showing headroom above
         your hair, your whole face and shoulders, your torso, and your hands entering the bottom edge
       - Report: is this the crop you want as the prominent hero portrait in Phase 2? If not, say what
         should change — higher, lower, tighter, wider — and it will be re-exported before Phase 2 starts.

    5. Nothing broke
       - Click around http://localhost:8080/ and http://localhost:8080/contato
       - Expected: both pages render normally, no error screen, no missing sections
       - Report: anything visibly wrong?

    One decision left open for you, from the Phase 1 discussion (D-07): the CV file kept its original
    name with spaces, so the URL is `/cv/CV%20MURILO%20ORTEGA%202026.pdf`. It works. If you would rather
    have a clean URL like `/cv/murilo-ortega-cv-2026.pdf`, say so now and it is a one-line follow-up.
    If you do not care, it stays as-is.
  </how-to-verify>
  <verify>
    <automated>echo "MANUAL GATE — no automated substitute. Task 1's E2E_AUDIT_OK is the machine-checkable half; ROADMAP Phase 1 criterion 1 requires a human click-test that cannot be automated. Blocked until the user reports outcomes for all 5 items."; exit 1</automated>
    <human-check>User reports an explicit outcome for each of the 5 checks and answers the D-07 CV-filename question.</human-check>
  </verify>
  <acceptance_criteria>
    - The user reports, per item, what they actually observed — items 1 through 5 each get an explicit outcome, not one blanket "approved"
    - Item 1 is confirmed as an actual downloaded, openable, correct PDF
    - Items 2 and 3 are confirmed as Murilo's own LinkedIn profile loading, not a 404 and not a different person
    - Item 4 records either approval of the crop, or a specific requested adjustment
    - If any item fails, the failure is recorded and the owning plan (01-01 for items 1-3, 01-02 for item 4) is re-run before Phase 1 is marked complete
    - The user's answer on the CV filename question (D-07) is recorded in the summary, including "no preference"
    - The dev server is stopped after the checkpoint resolves
  </acceptance_criteria>
  <resume-signal>Reply with the outcome of each of the 5 checks (e.g. "1 ok, 2 ok, 3 ok, 4 crop is too tight at the top, 5 ok"), plus your answer on the CV filename question. Type "approved" only if all five passed.</resume-signal>
  <done>All five checks have a recorded human outcome, any failure has been routed back to its owning plan and re-verified, and the D-07 answer is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature branch → `main` → live site | A push to `main` auto-deploys to the live Lovable-hosted Cloudflare Workers site with no staging step |
| local dev server → localhost | `vite dev` binds host `::`, so it is reachable from the local network, not just the loopback interface |
| verification claim → phase completion | Phase 2 builds on the assumption that these three fixes are real; an unverified pass here is a silent lie downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Repudiation | Human sign-off on Phase 1 | mitigate | The checkpoint requires a per-item observed outcome (5 explicit answers), not a blanket "approved". Blanket approval is not accepted as evidence for ROADMAP criterion 1, which demands an actual click-test. `workflow.auto_advance` is `false`, so this gate cannot be auto-passed. |
| T-01-09 | Elevation of Privilege | Accidental push to `main` during or after verification | mitigate | Task 1 asserts the current branch is `feature/home-recruiter-redesign` and that `main` still resolves to `9cbf2e5` before doing anything else, and halts on mismatch. No task in this plan pushes anything. The merge to `main` belongs to Phase 4 only, after its full checklist. |
| T-01-10 | Information Disclosure | `vite dev` binding host `::` exposes the unreleased branch, the CV PDF, and the new hero exports to the local network | accept | Short-lived local verification session on a developer machine; the same content is destined to be public anyway. Stop the dev server when the checkpoint resolves. |
| T-01-SC | Tampering | npm/bun package installs | accept | This plan installs nothing and modifies no source file. Asserted by `git status --porcelain package.json bun.lock package-lock.json` being empty in Task 1. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package surface exists. |
</threat_model>

<verification>
1. Task 1's automated audit printed `E2E_AUDIT_OK`
2. The user answered all 5 checkpoint items with observed outcomes
3. Every failing item was routed back to its owning plan (01-01 for the links, 01-02 for the crop) and re-verified
4. `git rev-parse main` still resolves to `9cbf2e5` — nothing reached the live-deploy branch
5. The dev server was stopped after the checkpoint resolved
</verification>

<success_criteria>
- FIX-01 verified by an actual browser click that produced the actual PDF (ROADMAP Phase 1 criterion 1, in the exact terms the roadmap demands)
- FIX-02 verified by the corrected URL loading Murilo's real profile from both the home page and the contact page (ROADMAP Phase 1 criterion 2)
- FIX-03 verified by the user approving the exported crop and the full-colour treatment, with `01-HERO-PHOTO.md` as the written record (ROADMAP Phase 1 criterion 3)
- The home page and the contact page still render with no errors
- `main` is untouched; the live site is unchanged; Phase 4 still owns the only merge
- The deferred CV-filename question (D-07) has an answer on the record, even if that answer is "no preference"
</success_criteria>

<output>
Create `.planning/phases/01-foundation-fixes-hero-decision/01-03-SUMMARY.md` when done.

Record in the summary:
- The exact status codes and grep counts from the Task 1 audit
- The user's verbatim outcome for each of the 5 checkpoint items
- The user's answer on the CV filename question (D-07), including "no preference"
- Any crop adjustment requested and whether it was re-exported
- Explicit confirmation that Phase 1 success criteria 1, 2 and 3 are all closed, so Phase 2 is unblocked
</output>
