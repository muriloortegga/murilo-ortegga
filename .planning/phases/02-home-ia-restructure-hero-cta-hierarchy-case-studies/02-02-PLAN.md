---
phase: 02-home-ia-restructure-hero-cta-hierarchy-case-studies
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/routes/index.tsx
  - .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-HEROGALLERY-AUDIT.md
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, ANTI-02, ANTI-04, ANTI-05]

must_haves:
  truths:
    - "A recruiter scanning the hero sees a real full-colour photo of Murilo as the hero's right-hand visual — not the 9-image scrolling gallery, not a grayscale background layer."
    - "The hero H1 renders two visually distinct lines: line 1 states a hireable role, line 2 states a delivered result — both are the exact strings approved in 02-COPY.md."
    - "LinkedIn, E-mail and CV sit together in one row as the first and heaviest CTA cluster in the hero; 'Ver Portfolio completo' is present below it as a plain text link."
    - "No WhatsApp link and no generic greeting exists anywhere in the hero."
    - "Every image the retired HeroGallery referenced is still reachable from /trabalho — proven by a written audit table, not asserted."
  artifacts:
    - path: "src/routes/index.tsx"
      provides: "Rebuilt hero section — dual-line H1, responsive hero portrait <picture>, three-tier recruiter-first CTA cluster"
      contains: "murilo-hero-960.jpg"
    - path: ".planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-HEROGALLERY-AUDIT.md"
      provides: "Reachability audit proving the 9 retired gallery images are all still accessible elsewhere (non-deletion constraint)"
      contains: "still reachable"
  key_links:
    - from: "src/routes/index.tsx"
      to: "public/assets/home/hero/murilo-hero-{480,960,1440}.{jpg,avif}"
      via: "<picture><source srcSet> + <img srcSet> (Phase 1 exported assets)"
      pattern: "murilo-hero-(480|960|1440)\\.(jpg|avif)"
    - from: "src/routes/index.tsx"
      to: "https://www.linkedin.com/in/murilo-ortega"
      via: "primary CTA anchor with target=_blank rel=noopener noreferrer"
      pattern: "linkedin\\.com/in/murilo-ortega"
    - from: ".planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md"
      to: "src/routes/index.tsx hero H1"
      via: "approved line_1/line_2 strings pasted verbatim as JSX text children"
      pattern: "<h1"
---

## Phase Goal

Source: `.planning/ROADMAP.md` Phase 2 `**Goal:**` line, quoted verbatim (the ROADMAP goal for this
phase is not written in canonical `As a / I want to / so that` syntax — it is reproduced as-is rather
than invented into that shape):

> A recruiter landing on the home page immediately sees who Murilo is (role + top result), a visually
> unambiguous CTA hierarchy favoring LinkedIn/e-mail/CV, and real proof of delivered work via 5 featured
> case studies — without losing access to any other project.

## Slice Outcome (this plan)

After this plan, a recruiter loading `/` sees a complete, working, recruiter-first hero: real photo,
two-line role+result headline, and a LinkedIn/E-mail/CV cluster they can click immediately. This is a
full vertical slice — the hero is shippable and visually verifiable on its own, without the case
section (plan `02-03`) existing yet.

<objective>
Rebuild the hero section of `src/routes/index.tsx` per `02-UI-SPEC.md` §1–3, §5–6: swap the
`HeroGallery` column for the Phase-1-exported responsive portrait, replace the single-line agency
headline with the user-approved two-line role+result H1, and restructure the CTA row into the
three-tier recruiter-first hierarchy (filled LinkedIn > outline E-mail/CV > text-link portfolio).
Then retire the now-unused gallery code and prove, in writing, that every image it referenced is
still reachable elsewhere.

Purpose: HERO-01/02/03/04 are the first-viewport half of Phase 2's goal — a recruiter must know who
Murilo is and be able to act on it inside 6-10 seconds. ANTI-02/04/05 constrain how: no greeting,
no WhatsApp, no new heavy animation.
Output: A rebuilt hero in `src/routes/index.tsx` plus `02-HEROGALLERY-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-PATTERNS.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md
@src/routes/index.tsx
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase on 2026-08-05. No exploration required. -->

Current `src/routes/index.tsx` import line 5 (extend, do not replace):
`import { ArrowRight, ChevronRight } from "lucide-react";`
Required final form adds three named icons: `ArrowRight, ChevronRight, Linkedin, Mail, Download`.
`ChevronRight` is currently unused in visible JSX — leave it in place, removing it is out of scope.

Existing CSS classes consumed by this plan (all already declared in `src/styles.css`, none modified):
- `.btn` (line 287) — base button, `display:inline-flex`, `gap: 0.5rem`
- `.btn-hero-primary` (line 543) — filled: `background: var(--foreground)`, `color: var(--background)`
- `.btn-hero-secondary` (line 566) — outline: transparent bg, `--foreground` text/border
- `.text-link` (line 451) — underlined uppercase text link, first use in `index.tsx` is this plan
- `.media-wrap` (used inline in `ProjectCard`, index.tsx line 223) — image container convention
- `.anim-fade-in`, `.delay-250`, `.delay-500` — existing CSS entrance animations (not new animation)

Phase-1 shipped asset paths, verified present on disk:
`public/assets/home/hero/murilo-hero-480.avif` (9.9KB), `-960.avif` (30KB), `-1440.avif` (72KB),
`murilo-hero-480.jpg` (29KB), `-960.jpg` (99KB), `-1440.jpg` (271KB)

Phase-1 corrected link values — copy verbatim, never retype from memory:
LinkedIn: `https://www.linkedin.com/in/murilo-ortega`
CV: `/cv/CV%20MURILO%20ORTEGA%202026.pdf`
E-mail: `contato@muriloortega.com` (matches the sitewide value in `src/routes/contato.tsx` line 49)

### Known out-of-scope string collisions in this file (do NOT "clean up")

These strings already appear more than once in `src/routes/index.tsx`. Verification in this plan is
deliberately scoped to the hero JSX range so these untouched occurrences do not produce false
failures — and the executor must leave every one of them exactly as-is:

| String | In-scope occurrence | Out-of-scope occurrence (leave untouched) |
|--------|---------------------|-------------------------------------------|
| `Design que confronta o comum` | hero `<p>` (line 277) — deleted by Task 1 | route `head()` SEO `description` (line 15) — Phase 4 owns SEO copy |
| `btn-hero-primary` | hero CTA (line 281) — becomes the LinkedIn button in Task 2 | `MethodsSection` "Ver Mais" button (line 386) — Phase 3 scope |
| `linkedin.com/in/murilo-ortega` | hero LinkedIn anchor — added by Task 2 | JSON-LD `sameAs` array (line 29) — already correct, untouched |
| `translateY(${scrollY * 0.1}px)` | hero text column (line 265) + CTA row (line 280) | none |

**Hero JSX range anchor for verification:** the hero begins at the `{/* Hero */}` comment (line 257)
and ends at the `{/* Brand Marquee */}` comment (line 307). Both comments must survive this plan —
the `<automated>` checks below use them as an `awk` range to scope greps to the hero only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild hero grid, two-line H1, and hero portrait column</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - `src/routes/index.tsx` lines 255–306 — the live hero JSX being replaced. Read it fresh; do NOT
      retype structure from memory (02-RESEARCH.md Pitfall 1).
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md` — the
      `## Approved Copy` section holds `line_1:` and `line_2:`. These two strings are the ONLY
      acceptable H1 text. If `status:` is not `approved`, stop and report — do not draft copy here.
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md`
      §1 (hero photo treatment, exact markup), §2 (grid + `sizes`), §6 (supporting paragraph removal),
      and the Typography table (H1 line 1 / line 2 clamp values).
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-PATTERNS.md`
      lines 94–163 — target JSX for the photo column and the H1, given verbatim.
  </read_first>
  <action>
    Edit the hero `<section>` in `src/routes/index.tsx` (currently lines 257–305) in place. Four
    changes, no rewrite of the surrounding `HomePage` function:

    1. Grid wrapper (line 260): change `grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-0 items-center
       min-h-[80vh]` to `grid-cols-1 lg:grid-cols-[1.4fr_1fr] gap-12 lg:gap-16 items-center
       min-h-[80vh]` (UI-SPEC §2). Text column stays first in DOM order, photo column second, no
       mobile reordering.

    2. Headline (current lines 271–275): replace the three-line `<h1>` ("Trabalho para transformar /
       marcas comuns em marcas / com impacto real") with the two-span structure from PATTERNS.md
       lines 153–161. Outer `<h1 className="anim-fade-in font-bold leading-[1.05] tracking-tighter">`;
       first child `<span className="block">` with inline `style={{ fontSize: "clamp(2rem, 3.5vw, 4rem)" }}`
       carrying `line_1` from 02-COPY.md; second child `<span className="block text-secondary
       font-medium italic mt-2">` with inline `style={{ fontSize: "clamp(1.5rem, 2.5vw, 2.75rem)" }}`
       carrying `line_2`. Paste both strings as plain JSX text children — never
       `dangerouslySetInnerHTML` (threat T-02-01). Preserve every accent and trailing period exactly
       as recorded in 02-COPY.md.

    3. Supporting paragraph (current lines 276–278, beginning "Design que confronta o comum e eleva o
       digital."): delete it entirely per UI-SPEC §6 — its message is absorbed by H1 line 2. This is a
       hero tagline rewrite, not project/page/asset deletion, so it does not conflict with the
       non-deletion constraint.
       **Delete ONLY the hero `<p>`.** The byte-identical sentence also lives in the route `head()`
       SEO `description` (line 15). That occurrence is out of scope — Phase 4 owns SEO/meta copy —
       and must survive untouched. After a correct edit, the file-wide count of this string is 1
       (the SEO description), and the hero-scoped count is 0; both are asserted below.

    4. Photo column: replace the entire right-column wrapper `<div>` (current lines 293–302 — the one
       with `relative h-[60vh] lg:h-screen w-full lg:w-[120%] lg:ml-8 overflow-hidden anim-fade-in
       delay-250 border-l border-border/10` plus its `scrollY` transform style, containing
       `<HeroGallery />`) with the photo card from UI-SPEC §1 / PATTERNS.md lines 96–124, verbatim:
       `<div className="media-wrap aspect-[4/5] rounded-2xl overflow-hidden border border-border/5
       max-w-[420px] mx-auto lg:mx-0 lg:ml-auto anim-fade-in delay-250">` wrapping a `<picture>` with
       an AVIF `<source>` (480/960/1440 `.avif` srcSet) and a JPEG `<img>` (`src` =
       `/assets/home/hero/murilo-hero-960.jpg`, 480/960/1440 `.jpg` srcSet). Both `sizes` attributes
       are the identical string `(min-width: 1024px) 420px, (min-width: 640px) 360px, 85vw`. The
       `<img>` carries `alt="Murilo Ortega"`, `width={960}`, `height={1200}`, `loading="eager"`,
       `fetchPriority="high"`, `className="w-full h-full object-cover"`.
       Do NOT carry over `lg:w-[120%]`, `h-screen`, `border-l`, or the gallery wrapper's `scrollY`
       transform. Do NOT route this image through `<ProjectMedia>` (01-HERO-PHOTO.md explicitly
       forbids it). `anim-fade-in delay-250` is carried over from the removed wrapper — it is an
       existing CSS class already applied to this exact column today, so it introduces no new
       animation (ANTI-05).

    Keep untouched: the left column's `lg:pr-20 py-12 lg:py-0 relative z-10` classes, the
    `{/* Hero */}` and `{/* Brand Marquee */}` JSX comments (they are the verification range anchors),
    and the `scrollY`-driven `translateY`/`opacity`/`blur` inline style block on the text column (lines
    263–270). That parallax is an established codebase pattern and is not in scope for removal.
    Do not add any Framer Motion `motion.*` element anywhere in the hero (ANTI-05).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'murilo-hero-960.jpg' src/routes/index.tsx && grep -q 'grid-cols-\[1.4fr_1fr\]' src/routes/index.tsx && grep -q 'fetchPriority="high"' src/routes/index.tsx && grep -q '{/\* Hero \*/}' src/routes/index.tsx && grep -q '{/\* Brand Marquee \*/}' src/routes/index.tsx && test "$(awk '/\{\/\* Hero \*\/\}/,/\{\/\* Brand Marquee \*\/\}/' src/routes/index.tsx | grep -c 'Design que confronta o comum' || true)" = "0" && test "$(grep -c 'Design que confronta o comum' src/routes/index.tsx || true)" = "1" && test "$(grep -c 'motion\.' src/routes/index.tsx || true)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `src/routes/index.tsx` contains all six hero asset filenames: `murilo-hero-480.avif`,
      `murilo-hero-960.avif`, `murilo-hero-1440.avif`, `murilo-hero-480.jpg`, `murilo-hero-960.jpg`,
      `murilo-hero-1440.jpg`
    - The `<img>` in the hero `<picture>` has `loading="eager"` AND `fetchPriority="high"` AND
      `width={960}` AND `height={1200}`
    - Both `sizes` attributes equal `(min-width: 1024px) 420px, (min-width: 640px) 360px, 85vw`
    - The hero `<h1>` contains exactly two `<span className="block...">` children, and their text
      matches `line_1` / `line_2` from `02-COPY.md` character-for-character (including accents)
    - Hero-scoped removal of the supporting paragraph, asserted two ways (baseline before this task:
      hero-scoped 1, file-wide 2):
      `awk '/\{\/\* Hero \*\/\}/,/\{\/\* Brand Marquee \*\/\}/' src/routes/index.tsx | grep -c "Design que confronta o comum"`
      returns 0, AND `grep -c "Design que confronta o comum" src/routes/index.tsx` returns 1 — the
      surviving occurrence being the `head()` SEO `description` on line 15, which is out of scope
    - `grep -ci "olá, sou o murilo" src/routes/index.tsx` returns 0 (ANTI-02)
    - `grep -c "motion\." src/routes/index.tsx` returns 0 and no `framer-motion` import was added (ANTI-05)
    - `grep -c "dangerouslySetInnerHTML" src/routes/index.tsx` returns 0
    - Both range-anchor comments survive: `grep -c '{/\* Hero \*/}'` and `grep -c '{/\* Brand Marquee \*/}'`
      each return 1
    - `git branch --show-current` still outputs `feature/home-recruiter-redesign`
  </acceptance_criteria>
  <done>Hero grid is 1.4fr/1fr, the H1 renders the two approved lines, the agency tagline paragraph is gone from the hero (and only from the hero — the SEO description is untouched), and the right column is the eager-loaded responsive portrait card instead of the gallery. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build the three-tier recruiter-first CTA cluster</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - `src/routes/index.tsx` lines 279–291 — the current CTA row (`Ver Portfolio` / `Ver mais sobre mim` /
      `Baixar CV`) being replaced, including its inline `style={{ transform: ... }}` parallax on line 280,
      and line 5 — the `lucide-react` import to extend.
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md` §3 —
      the locked three-tier structure and the locked CTA labels in the Copywriting Contract table.
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-PATTERNS.md`
      lines 128–150 — target CTA JSX verbatim, plus the "copy these values verbatim" warning list.
    - `src/routes/contato.tsx` line 49 — confirms the sitewide `mailto:contato@muriloortega.com` value.
    - `src/components/Header.tsx` — confirms the sitewide `target="_blank"` + `rel="noopener noreferrer"` pairing.
  </read_first>
  <action>
    Extend the `lucide-react` import on line 5 to `import { ArrowRight, ChevronRight, Linkedin, Mail,
    Download } from "lucide-react";`. Keep `ChevronRight` even though it appears unused — removing it
    is outside this phase's scope.

    Replace the current CTA `<div>` (lines 279–291) with two sibling blocks, exactly per UI-SPEC §3:

    Block 1 — the primary cluster, `<div className="mt-12 flex flex-wrap gap-4 anim-fade-in delay-500">`
    containing three anchors in this order:
      a. LinkedIn — `<a href="https://www.linkedin.com/in/murilo-ortega" target="_blank"
         rel="noopener noreferrer" className="btn btn-hero-primary gap-2 text-xs">` with a
         `<Linkedin size={16} />` icon and the locked label `Conectar no LinkedIn`. This is the ONLY
         filled button in the hero.
      b. E-mail — `<a href="mailto:contato@muriloortega.com" className="btn btn-hero-secondary gap-2
         text-xs">` with `<Mail size={16} />` and the locked label `Enviar E-mail`.
      c. CV — `<a href="/cv/CV%20MURILO%20ORTEGA%202026.pdf" download className="btn btn-hero-secondary
         gap-2 text-xs">` with `<Download size={16} />` and the locked label `Baixar CV`.

    Block 2 — the demoted tertiary link, `<div className="mt-8 anim-fade-in delay-500">` containing
    `<Link to="/trabalho" className="text-link">` with the label `Ver Portfolio completo`. The word
    "completo" is required by HERO-04 and is the exact wording-drift trap named in 02-RESEARCH.md
    Pitfall 5 — the current code says only "Ver Portfolio".

    **Preserve the scroll parallax on both blocks.** The CTA row being replaced (line 280) carries
    `style={{ transform: `translateY(${scrollY * 0.1}px)` }}`, matching the identical transform on the
    headline block above it (line 265) so the whole left column moves as one unit. Carry that exact
    inline style onto BOTH Block 1 and Block 2 — dropping it would leave the CTAs visually static
    while the headline above them translates on scroll, a desync regression that no requirement asks
    for. This is a carried-over existing inline style, not a new animation, so ANTI-05 is unaffected.
    After this task the file contains exactly 3 occurrences of that transform string: headline block,
    Block 1, Block 2.

    The old `<Link to="/sobre">Ver mais sobre mim</Link>` is dropped from the hero; `/sobre` remains
    reachable from the site `Header` nav and `Footer`, so no page becomes unreachable.

    Constraints:
    - `text-xs` on each CTA anchor is deliberate — it overrides `.btn-hero-*`'s `font-size: 0.8125rem`
      via Tailwind's utilities-after-components cascade so all CTA labels sit at the 12px Label tier.
      Do not edit `src/styles.css` to achieve this.
    - Do NOT add a new colour, gradient, shadow, or a new `.btn-*` class. Hierarchy is expressed only
      as filled > outline > text-link (02-RESEARCH.md anti-pattern list).
    - Do NOT render a WhatsApp link, icon, or `wa.me` href anywhere in the hero (ANTI-04). Phase 3
      owns where WhatsApp reappears, subordinate, in the final contact block.
    - The `rel="noopener noreferrer"` on the LinkedIn anchor is non-negotiable (threat T-02-03).
    - Do NOT touch `MethodsSection`'s "Ver Mais" button (line 386), which also uses `btn-hero-primary`.
      It is Phase 3 scope. After this task the file-wide `btn-hero-primary` count is 2 (hero LinkedIn +
      that untouched button); only the hero-scoped count is 1. Both are asserted below.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q '{/\* Hero \*/}' src/routes/index.tsx && grep -q '{/\* Brand Marquee \*/}' src/routes/index.tsx && grep -q 'https://www.linkedin.com/in/murilo-ortega' src/routes/index.tsx && grep -q 'mailto:contato@muriloortega.com' src/routes/index.tsx && grep -q '/cv/CV%20MURILO%20ORTEGA%202026.pdf' src/routes/index.tsx && grep -q 'Ver Portfolio completo' src/routes/index.tsx && test "$(awk '/\{\/\* Hero \*\/\}/,/\{\/\* Brand Marquee \*\/\}/' src/routes/index.tsx | grep -c 'btn-hero-primary' || true)" = "1" && test "$(awk '/\{\/\* Hero \*\/\}/,/\{\/\* Brand Marquee \*\/\}/' src/routes/index.tsx | grep -B1 -A3 'linkedin\.com/in/murilo-ortega' | grep -c 'btn-hero-primary' || true)" = "1" && test "$(grep -c 'btn-hero-primary' src/routes/index.tsx || true)" = "2" && test "$(grep -cF 'translateY(${scrollY * 0.1}px)' src/routes/index.tsx || true)" = "3" && test "$(grep -c 'wa.me\|whatsapp\|WhatsApp' src/routes/index.tsx || true)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Hero-scoped filled-button count is exactly 1 (HERO-03 single dominant CTA):
      `awk '/\{\/\* Hero \*\/\}/,/\{\/\* Brand Marquee \*\/\}/' src/routes/index.tsx | grep -c "btn-hero-primary"`
      returns 1, and that occurrence sits on the LinkedIn anchor (proven by the `-B1 -A3` adjacency
      grep in `<verify>` also returning 1)
    - `grep -c "btn-hero-primary" src/routes/index.tsx` returns exactly 2 file-wide — the hero LinkedIn
      button plus the untouched `MethodsSection` "Ver Mais" button (Phase 3 scope). A count of 3+ means
      a stray filled button was added; a count of 1 means `MethodsSection` was wrongly edited
    - `grep -c "btn-hero-secondary" src/routes/index.tsx` returns exactly 2 (E-mail + CV; no other
      occurrence exists in this file)
    - `grep -c "text-link" src/routes/index.tsx` returns exactly 1 (Ver Portfolio completo, HERO-04)
    - The LinkedIn href exact-matches `https://www.linkedin.com/in/murilo-ortega` and the same anchor
      carries both `target="_blank"` and `rel="noopener noreferrer"`. Note the JSON-LD `sameAs` array
      (line 29) contains the same URL and is intentionally untouched
    - The CV href exact-matches `/cv/CV%20MURILO%20ORTEGA%202026.pdf`
    - `grep -c "Ver Portfolio completo" src/routes/index.tsx` returns 1 and
      `grep -c '>Ver Portfolio<' src/routes/index.tsx` returns 0
    - Parallax preserved: `grep -cF 'translateY(${scrollY * 0.1}px)' src/routes/index.tsx` returns 3
      (headline block + CTA Block 1 + CTA Block 2)
    - `grep -ci "wa.me\|whatsapp" src/routes/index.tsx` returns 0 (ANTI-04)
    - `grep -c "Ver mais sobre mim" src/routes/index.tsx` returns 0, and `/sobre` still appears in
      `src/components/Header.tsx` (page stays reachable)
    - No new class was added to `src/styles.css`: `git diff --stat src/styles.css` is empty
  </acceptance_criteria>
  <done>The hero renders one filled LinkedIn button, two outline buttons (E-mail, CV) in the same row, and a text-link "Ver Portfolio completo" below — all three CTA blocks keeping the existing scroll parallax. No WhatsApp anywhere. MethodsSection and styles.css untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Retire HeroGallery code and publish the image-reachability audit</name>
  <files>src/routes/index.tsx, .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-HEROGALLERY-AUDIT.md</files>
  <read_first>
    - `src/routes/index.tsx` lines 125–182 — `galleryImages` (125–136), `galleryAlts` (137–148) and
      `HeroGallery()` (149–182), the code being retired. Read the 9 image paths from the live file,
      not from this plan, in case they drifted.
    - `src/routes/trabalho.tsx` lines 44–70 — the full project list that keeps every one of those
      images reachable.
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md` §5 —
      the pre-approved "removal is reorganization, not deletion" ruling, and its explicit instruction
      to flag the removal for visibility rather than do it silently.
    - `.planning/PROJECT.md` — the non-negotiable "nothing existing may be deleted" constraint this
      audit exists to satisfy.
  </read_first>
  <action>
    First write the audit, then remove the code — in that order, so the evidence exists before the
    deletion.

    Create `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-HEROGALLERY-AUDIT.md`
    with YAML frontmatter (`phase: 02`, `plan: 02`, `constraint: no-content-deletion`) and a body
    containing one markdown table with columns: `Image path` | `Project` | `Still reachable at` |
    `Verified by`. One row per entry in the live `galleryImages` array — 9 rows expected:
    `thumbnails/symplice.jpg`, `thumbnails/social/kapyi.jpg`, `thumbnails/solid.jpg`,
    `thumbnails/natrave.jpg`, `thumbnails/kmillion.jpg`, `thumbnails/social/talk2buy.jpg`,
    `thumbnails/social/maxi.jpg`, `thumbnails/social/milgrows.jpg`, `thumbnails/social/evidive.jpg`.
    For each row, the `Still reachable at` cell must name the `/trabalho` category entry that renders
    that exact path plus the project's own route (e.g. `/trabalho` "Id Visual" + `/symplice`), and the
    `Verified by` cell must record the actual grep used against `src/routes/trabalho.tsx`. If any path
    does NOT appear in `trabalho.tsx`, do not remove the gallery — stop and report the orphan.

    Add a short `## Ruling` section stating: the image assets in `public/` are untouched; only the
    hero-gallery UI pattern is retired; UI-SPEC §5 pre-approved this as reorganization; and
    `src/styles.css`'s `.hero-gallery*` rules (lines 504–541) are intentionally left in place because
    `styles.css` is out of scope for this phase.

    Then, in `src/routes/index.tsx`, delete the `galleryImages` array, the `galleryAlts` array, and
    the `HeroGallery()` function. The `<HeroGallery />` call site was already replaced in Task 1.
    Do not leave a commented-out copy and do not leave a comment mentioning `HeroGallery` — the audit
    file is the record. Do not touch `src/styles.css`. Do not touch `src/routes/trabalho.tsx` or any
    file under `public/`.

    Commit both files together on `feature/home-recruiter-redesign` with message
    `feat(02-02): rebuild hero with portrait, dual headline and recruiter-first CTA cluster`.
    Never commit to `main`.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-HEROGALLERY-AUDIT.md && for p in symplice.jpg social/kapyi.jpg solid.jpg natrave.jpg kmillion.jpg social/talk2buy.jpg social/maxi.jpg social/milgrows.jpg social/evidive.jpg; do grep -q "thumbnails/$p" src/routes/trabalho.tsx || { echo "UNREACHABLE:$p"; exit 1; }; done && test "$(grep -v '{/\*' src/routes/index.tsx | grep -v '^\s*//' | grep -c 'HeroGallery\|galleryImages\|galleryAlts' || true)" = "0" && test -z "$(git diff --stat src/styles.css)" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `02-HEROGALLERY-AUDIT.md` exists and its table has exactly 9 data rows, one per live
      `galleryImages` entry
    - Every one of the 9 paths returns a match from `grep "thumbnails/<path>" src/routes/trabalho.tsx`
    - `grep -c "HeroGallery\|galleryImages\|galleryAlts" src/routes/index.tsx` (comment lines filtered)
      returns 0
    - `git diff --stat src/styles.css` is empty (stylesheet untouched)
    - `git status --porcelain public/` is empty (zero image assets added, moved, or deleted)
    - `git status --porcelain src/routes/trabalho.tsx` is empty
    - `npx tsc --noEmit` exits 0 and `npm run lint` reports no new errors versus the pre-task baseline
    - `git log --oneline -1` shows the commit on branch `feature/home-recruiter-redesign`; `main` is
      unchanged
  </acceptance_criteria>
  <done>The gallery code is gone from index.tsx, the audit file proves all 9 images remain reachable via /trabalho and their project routes, no asset or stylesheet was touched, and the work is committed on the feature branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| origin tab → external site (LinkedIn) | A `target="_blank"` anchor opens a third-party origin that can reach back via `window.opener` unless severed. |
| public web → rendered e-mail address | A bare `mailto:` address in SSR'd HTML is scrapeable by crawlers. |
| planning artifact → rendered DOM | User-authored headline copy from `02-COPY.md` becomes JSX text in the H1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Hero LinkedIn anchor in `src/routes/index.tsx` | mitigate | Anchor must carry `rel="noopener noreferrer"` alongside `target="_blank"` — severs `window.opener`, preventing reverse-tabnabbing. Enforced by Task 2 acceptance criteria; matches the grep-verified sitewide convention in `Header.tsx`/`contato.tsx`. |
| T-02-04 | Information Disclosure | Hero `mailto:contato@muriloortega.com` | accept | Bare `mailto:` is the already-accepted sitewide pattern (`contato.tsx` line 49). Introducing obfuscation here would create an inconsistent, higher-maintenance surface for a low-severity spam risk on an address already public elsewhere on the site. |
| T-02-01 | Tampering | Approved headline strings rendered in the hero H1 | mitigate | Strings enter as plain JSX text children, which React auto-escapes. `dangerouslySetInnerHTML` is forbidden for this content and asserted at 0 occurrences in Task 1's acceptance criteria. |
| T-02-05 | Denial of Service | Hero portrait payload on first paint | mitigate | Only the 4:5 portrait gets `loading="eager"`/`fetchPriority="high"`; largest tier is 271KB JPEG / 72KB AVIF (verified on disk). No other image in this plan is eager-loaded. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this plan. `02-RESEARCH.md` "Package Legitimacy Audit" records zero external packages added; `lucide-react@0.575.0` is already installed and its `linkedin.js`/`mail.js`/`download.js` icon modules were verified present on disk. Nothing to audit. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npm run lint` shows no new errors versus the pre-plan baseline
3. `npm run dev` boots on port 8080 and `/` renders without a console error
4. `curl -s http://localhost:8080/ | grep -c murilo-hero` returns a non-zero count (SSR emits the portrait)
5. Hero-scoped filled-CTA check returns 1:
   `awk '/\{\/\* Hero \*\/\}/,/\{\/\* Brand Marquee \*\/\}/' src/routes/index.tsx | grep -c "btn-hero-primary"`
   — file-wide the count is 2, the second being `MethodsSection`'s untouched "Ver Mais" button
6. `grep -ci "wa.me\|whatsapp" src/routes/index.tsx` returns 0
7. `grep -c "Design que confronta o comum" src/routes/index.tsx` returns 1 (SEO description only —
   the hero-scoped count is 0)
8. `git diff --stat src/styles.css` and `git status --porcelain public/` are both empty
9. `git branch --show-current` is `feature/home-recruiter-redesign`; no commit reached `main`
</verification>

<success_criteria>
- HERO-01: the hero H1 renders two lines — role (line 1) and delivered result (line 2) — using the exact 02-COPY.md approved strings
- HERO-02: a full-colour foreground portrait of Murilo occupies the hero's right column, eager-loaded, responsive across 3 widths in AVIF + JPEG — not a grayscale background layer, not the 9-image gallery
- HERO-03: LinkedIn (filled) + E-mail (outline) + CV (outline) form one dominant first CTA row — the only filled button inside the hero range
- HERO-04: "Ver Portfolio completo" is present as a text-link, visually subordinate, still pointing at `/trabalho`
- ANTI-02: no generic greeting in the hero
- ANTI-04: zero WhatsApp references in the hero
- ANTI-05: zero new animation — no Framer Motion added, only pre-existing `anim-fade-in`/`scroll-reveal` classes and the carried-over `scrollY` inline transform
- Out-of-scope collisions untouched: the `head()` SEO description still carries "Design que confronta o comum" and `MethodsSection` still carries its `btn-hero-primary` "Ver Mais" button
- Non-deletion constraint: 02-HEROGALLERY-AUDIT.md proves all 9 retired gallery images remain reachable; zero files under `public/` changed
</success_criteria>

<output>
Create `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-02-SUMMARY.md` when done.
Record: the final H1 line 1 / line 2 strings as shipped, the final CTA label + href pairs, confirmation
that `src/styles.css` and `public/` were untouched, and the path to `02-HEROGALLERY-AUDIT.md` so plan
`02-03`'s executor knows the hero is settled and only the case section remains.
</output>
