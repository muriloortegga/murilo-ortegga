---
phase: 02-home-ia-restructure-hero-cta-hierarchy-case-studies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md
autonomous: false
requirements: [HERO-01, ANTI-02]

must_haves:
  truths:
    - "The exact strings for hero H1 line 1 (role) and line 2 (result) are user-approved before any hero JSX is edited."
    - "The approved strings live in one file that plan 02-02 can read verbatim — no executor re-derives or re-drafts copy."
    - "No approved option contains a generic unearned greeting (ANTI-02)."
  artifacts:
    - path: ".planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md"
      provides: "User-approved hero headline copy record (line 1 + line 2), plus the 3 drafts that were presented"
      contains: "status: approved"
  key_links:
    - from: ".planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md"
      to: "src/routes/index.tsx hero H1"
      via: "plan 02-02 reads the approved line_1/line_2 strings verbatim"
      pattern: "line_1:|line_2:"
---

## Phase Goal

Source: `.planning/ROADMAP.md` Phase 2 `**Goal:**` line, quoted verbatim (the ROADMAP goal for this
phase is not written in canonical `As a / I want to / so that` syntax — it is reproduced as-is rather
than invented into that shape):

> A recruiter landing on the home page immediately sees who Murilo is (role + top result), a visually
> unambiguous CTA hierarchy favoring LinkedIn/e-mail/CV, and real proof of delivered work via 5 featured
> case studies — without losing access to any other project.

## Slice Outcome (this plan)

After this plan, the exact words the recruiter will read in the hero are decided and written down.
No code changes yet — this is the blocking copy-approval gate that `02-02` depends on.

<objective>
Present the 3 hero-headline draft pairs from `02-UI-SPEC.md`'s Copywriting Contract to the user,
capture the selection (or the user's own edit), and record the final exact strings in a single
decision record so the hero build in plan `02-02` never invents or re-drafts marketing copy.

Purpose: `02-UI-SPEC.md` marks the hero headline **UNCONFIRMED** ("present these to the user at the
plan-review or execution checkpoint before shipping. Do not ship silently"), and `02-CONTEXT.md`
lists exact hero headline copy under Deferred Ideas. Shipping an unreviewed headline would violate
both.
Output: `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify branch state and stage the draft record</name>
  <read_first>
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md` — the "Copywriting Contract" table rows "Hero role line (H1 line 1)" and "Hero result line (H1 line 2)" hold the 3 draft pairs, verbatim
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CONTEXT.md` — "Claude's Discretion" → Hero headline copy constraints (standard market job terminology, no invented titles)
    - `.planning/REQUIREMENTS.md` — HERO-01 and ANTI-02 wording
  </read_first>
  <action>
    First, assert the working branch. Run `git branch --show-current` and confirm it outputs exactly
    `feature/home-recruiter-redesign`. This branch already exists from Phase 1 and already carries
    Phase 1's commits — do NOT create a new branch, do NOT branch off `main`, do NOT commit to `main`.
    If the current branch is anything else, stop and report rather than switching or creating.

    Then create `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md`
    with YAML frontmatter containing `status: pending-approval`, `phase: 02`, and `requirement: HERO-01`.

    Body must transcribe all three draft pairs verbatim from `02-UI-SPEC.md`'s Copywriting Contract —
    do not paraphrase, do not re-draft, do not add a fourth option:
    - Draft A (marked recommended in UI-SPEC): line 1 "Diretor de Arte & Designer de Marca";
      line 2 "8 anos transformando marcas comuns em marcas com resultado real."
    - Draft B: line 1 "Branding, Social Media & Direção de Arte"; line 2 "Identidade e conteúdo que já
      geraram +2.250 seguidores e posicionamento premium para marcas como NaTrave e Kapyi."
    - Draft C: line 1 "Designer de Marca & Social Media"; line 2 "De NaTrave a Solid+: 8 anos entregando
      marca, conteúdo e presença digital com resultado."

    For each draft add a one-line ANTI-02 compliance note stating that the line-1 text is a market-standard
    role title and contains no greeting form ("Olá", "Oi", "sou o Murilo", "Hi, I'm").

    Add an "Option D — user's own wording" placeholder section for the case where the user supplies
    their own line 1 / line 2 at the checkpoint.

    Do not edit `src/routes/index.tsx` in this plan. This plan is documentation-only.
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "feature/home-recruiter-redesign" && test -f .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md && grep -q "status: pending-approval" .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md && grep -c "Diretor de Arte & Designer de Marca" .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` outputs exactly `feature/home-recruiter-redesign`
    - `git log --oneline -1 main` is unchanged from before this task (no commit reached `main`)
    - `02-COPY.md` exists and its frontmatter contains `status: pending-approval`
    - `02-COPY.md` contains all three exact line-1 strings: `Diretor de Arte & Designer de Marca`, `Branding, Social Media & Direção de Arte`, `Designer de Marca & Social Media`
    - `02-COPY.md` contains all three exact line-2 strings, each matching the UI-SPEC Copywriting Contract character-for-character
    - `git status --porcelain src/` returns empty (no source file touched by this plan)
  </acceptance_criteria>
  <done>02-COPY.md exists in pending-approval state with all 3 verbatim drafts; branch confirmed as feature/home-recruiter-redesign; zero source files modified.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Which hero headline pair (H1 line 1 = role, H1 line 2 = delivered result) ships on the home page?</decision>
  <context>
This is the blocking human copy-approval gate. `02-UI-SPEC.md` marks the hero headline UNCONFIRMED and
`02-CONTEXT.md` defers exact hero headline copy to the user. Plan `02-02` writes this text into the
hero H1 — it must not proceed on an unapproved string.

Constraints on any answer:
- Line 1 must be a hireable role in standard market terminology (HERO-01). No invented job titles.
- Line 2 must state a delivered result (HERO-01).
- Neither line may be a generic greeting such as "Olá, sou o Murilo" (ANTI-02).
- If the user picks Draft B, the "+2.250 seguidores" figure traces to `src/routes/natrave.tsx` line 69
  (`followers={2250}`) — it is a real metric, not fabricated.
  </context>
  <options>
    <option id="draft-a">
      <name>Draft A (UI-SPEC recommended)</name>
      <pros>Shortest, cleanest scan. Line 1 is two unambiguous market job titles. Line 2 is a clean 8-year positioning claim with no number to defend.</pros>
      <cons>Line 2 has no hard proof point — the numbers only appear later, in the case cards.</cons>
    </option>
    <option id="draft-b">
      <name>Draft B</name>
      <pros>Line 2 carries a real, sourceable metric (+2.250 seguidores) and names two real clients — strongest proof density in the first viewport.</pros>
      <cons>Longest line 2; risks wrapping to 3+ lines on a 375px viewport. Line 1 reads more as a skill list than a job title.</cons>
    </option>
    <option id="draft-c">
      <name>Draft C</name>
      <pros>Line 2 name-drops two cases and links the hero to the case section directly below. Line 1 is a compact standard title.</pros>
      <cons>Middle option — neither the shortest nor the most quantified.</cons>
    </option>
    <option id="draft-d">
      <name>User's own wording</name>
      <pros>Exactly what Murilo wants to be called and known for.</pros>
      <cons>Must still satisfy HERO-01 (role + result) and ANTI-02 (no generic greeting) — will be checked against those before recording.</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md` — the staged drafts written in Task 1
  </read_first>
  <acceptance_criteria>
    - User has responded with exactly one of: `draft-a`, `draft-b`, `draft-c`, or their own line 1 + line 2 text
    - If the user supplied their own text, both a line 1 and a line 2 were provided (not just one)
    - The selected line 1 contains no greeting token (case-insensitive check for `olá`, `oi,`, `sou o murilo`, `hi, i'm`)
  </acceptance_criteria>
  <resume-signal>Reply with `draft-a`, `draft-b`, `draft-c`, or paste your own two lines labelled "linha 1:" and "linha 2:"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the approved copy as the single source of truth</name>
  <read_first>
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md` — the pending-approval record to be finalized
  </read_first>
  <action>
    Update `02-COPY.md` in place. Change frontmatter `status: pending-approval` to `status: approved`,
    add `approved_by: user`, add `approved_on:` with today's date, and add `selected: <the option id the
    user chose, or `draft-d` if they supplied their own text>`.

    Add a section titled `## Approved Copy` containing exactly two labelled lines in the form
    `line_1: "<exact string>"` and `line_2: "<exact string>"`, using the user's chosen text
    character-for-character including accents and trailing punctuation. These two strings are what
    plan `02-02` will paste into the hero H1 — nothing downstream re-derives them.

    Keep the three original drafts in the file below the approved section under a
    `## Drafts Presented` heading, so the rejected options remain on record.

    Add a `## HERO-01 / ANTI-02 Compliance` section asserting, in one line each, that line 1 states a
    hireable role in standard market terminology and that neither line is a generic greeting.

    Commit the file with message `docs(02-01): record approved hero headline copy` on
    `feature/home-recruiter-redesign`. Do not push to `main`.
  </action>
  <verify>
    <automated>grep -q "status: approved" .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md && grep -q "^line_1:" .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md && grep -q "^line_2:" .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md && grep -qi "olá, sou o murilo" .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-COPY.md && echo FAIL_ANTI02 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `02-COPY.md` frontmatter contains `status: approved`, `approved_by: user`, and a `selected:` value
    - The file contains a line starting `line_1:` and a line starting `line_2:`, each with a quoted string
    - The `line_1:` and `line_2:` values match the user's checkpoint response character-for-character
    - `grep -ci "olá, sou o murilo" 02-COPY.md` returns 0 for the `## Approved Copy` section (ANTI-02)
    - The three original drafts are still present in the file under `## Drafts Presented`
    - `git log --oneline -1` shows the commit `docs(02-01): record approved hero headline copy` on branch `feature/home-recruiter-redesign`
  </acceptance_criteria>
  <done>02-COPY.md is approved, contains the two exact final strings under line_1/line_2, retains the rejected drafts, and is committed on the feature branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → planning artifact | Free-text copy supplied by the user is written into a markdown record; it is later rendered as static JSX text by React (auto-escaped). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | User-supplied headline text later rendered in `src/routes/index.tsx` H1 | mitigate | Text is rendered as a JSX text child, which React auto-escapes. Plan 02-02 forbids `dangerouslySetInnerHTML` for this content — headline strings go in as plain JSX children only. |
| T-02-02 | Repudiation | Which headline the user approved | mitigate | `02-COPY.md` records `selected:`, `approved_by: user`, `approved_on:` and retains the rejected drafts; committed to git for an auditable trail. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this phase. `02-RESEARCH.md` "Package Legitimacy Audit" states zero external packages are added; all icons/classes reuse installed `lucide-react@0.575.0` and existing CSS. Nothing to audit. |
</threat_model>

<verification>
1. `git branch --show-current` → `feature/home-recruiter-redesign` (Phase 1 branch reused, not recreated)
2. `git status --porcelain src/` → empty (this plan touches zero source files)
3. `02-COPY.md` has `status: approved` with `line_1:` and `line_2:` present
4. Blocking human copy-approval gate was answered by the user, not auto-selected
</verification>

<success_criteria>
- The exact hero H1 line 1 and line 2 strings are user-approved and recorded in `02-COPY.md`
- HERO-01 is satisfiable by plan 02-02 without any further copy invention
- ANTI-02 is asserted in writing against the approved copy
- Zero source files changed; zero commits on `main`
</success_criteria>

<output>
Create `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-01-SUMMARY.md` when done.
Include the approved `line_1` / `line_2` strings verbatim in the summary so plan 02-02's executor sees
them without re-reading 02-COPY.md.
</output>
