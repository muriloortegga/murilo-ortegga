---
phase: 02-home-ia-restructure-hero-cta-hierarchy-case-studies
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md
autonomous: false
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, CASE-01, CASE-02, CASE-03, ANTI-02, ANTI-03, ANTI-04, ANTI-05]

must_haves:
  truths:
    - "Murilo has personally looked at the rebuilt home page in a browser and signed off, or listed exactly what is wrong."
    - "All 5 ROADMAP Phase 2 success criteria have an explicit PASS or FAIL recorded against them — none is assumed."
    - "The squint test confirms exactly one visually dominant CTA style in the hero."
    - "No link that worked before the restructure is broken after it."
    - "Any FAIL is written down as a routable gap with the plan that owns the fix, not silently patched during the review."
  artifacts:
    - path: ".planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md"
      provides: "Automated pre-flight results plus the human verdict against each of the 5 ROADMAP success criteria"
      contains: "verdict:"
  key_links:
    - from: ".planning/ROADMAP.md Phase 2 Success Criteria"
      to: ".planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md"
      via: "one PASS/FAIL row per criterion, human-confirmed"
      pattern: "SC-[1-5]"
    - from: "02-UAT.md FAIL rows"
      to: "gap-closure planning"
      via: "each FAIL names the owning plan (02-02 hero or 02-03 cases) for /gsd:plan-phase --gaps"
      pattern: "owner: 02-0[23]"
---

## Phase Goal

Source: `.planning/ROADMAP.md` Phase 2 `**Goal:**` line, quoted verbatim (the ROADMAP goal for this
phase is not written in canonical `As a / I want to / so that` syntax — it is reproduced as-is rather
than invented into that shape):

> A recruiter landing on the home page immediately sees who Murilo is (role + top result), a visually
> unambiguous CTA hierarchy favoring LinkedIn/e-mail/CV, and real proof of delivered work via 5 featured
> case studies — without losing access to any other project.

## Slice Outcome (this plan)

After this plan, the rebuilt home page has been looked at by a human in a real browser and judged
against the phase's own definition of done — so Phase 3 starts from a verified base, not an assumed one.

<objective>
Run an automated pre-flight sweep over the rebuilt home page, then walk Murilo through a blocking
visual review against ROADMAP Phase 2's five success criteria, and record the verdict.

Purpose: Phase 2 is the first phase in this milestone with real visual output. Its success criteria
are stated in recruiter-perception terms ("scanning the hero for 6-10 seconds", "visually dominant",
"squint test") — those cannot be settled by grep. Phase 1's equivalent checkpoint (plan 01-03) caught
its issues at this exact gate; this is the same gate for the visible work.
Output: `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-02-SUMMARY.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight sweep and UAT scaffold</name>
  <files>.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — Phase 2 "Success Criteria" list, all 5 items, quoted verbatim into the scaffold
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-02-SUMMARY.md` — what the hero actually shipped (final H1 strings, final CTA hrefs)
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-03-SUMMARY.md` — the 5 shipped case entries and their P/A/R sentences
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-HEROGALLERY-AUDIT.md` and `02-CASE-SOURCES.md` — the non-deletion and no-fabrication evidence the human review spot-checks
  </read_first>
  <action>
    Run the automated sweep first, capturing each command's actual output — do not pre-write results.

    Build and lint: `npx tsc --noEmit`, then `npm run lint`.

    Dev server: start `npm run dev` (port 8080) in the background, wait until it responds, then
    collect these counts from the server-rendered HTML via `curl -s http://localhost:8080/`:
      - occurrences of `murilo-hero` (expect at least 1 — hero portrait present, HERO-02)
      - occurrences of `linkedin.com/in/murilo-ortega` (expect at least 1 — HERO-03)
      - occurrences of `mailto:contato@muriloortega.com` (expect at least 1 — HERO-03)
      - occurrences of `CV%20MURILO%20ORTEGA%202026.pdf` (expect at least 1 — HERO-03)
      - occurrences of `Ver Portfolio completo` (expect 1 — HERO-04)
      - occurrences of `Resultado:` (expect 5 — CASE-01/CASE-02)
      - occurrences of `hero-gallery-track` (expect 0 — gallery retired)
      - case-insensitive occurrences of `whatsapp` and `wa.me` (expect 0 — ANTI-04)
      - case-insensitive occurrences of `olá, sou o murilo` (expect 0 — ANTI-02)
    Also fetch the 6 hero asset URLs (`/assets/home/hero/murilo-hero-{480,960,1440}.{jpg,avif}`) and
    the 5 card image URLs with `curl -s -o /dev/null -w "%{http_code}"` — every one must return 200.
    Then fetch `/trabalho`, `/natrave`, `/symplice`, `/maxi`, `/solid`, `/kapyi`, `/kmillion`,
    `/evidive`, `/talk2buy`, `/sobre` and confirm each returns 200 (no link broke — CASE-03 and the
    non-deletion constraint).

    Then create `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md`
    with YAML frontmatter (`phase: 02`, `plan: 04`, `status: pending-human-review`, `verdict: pending`)
    and these sections:
      - `## Automated Pre-flight` — a table of every command above with its actual output and PASS/FAIL
      - `## Success Criteria` — five rows `SC-1` through `SC-5`, each quoting the ROADMAP criterion
        verbatim, each with empty `Human verdict` and `Notes` cells for Task 2 to fill
      - `## Evidence Index` — links to `02-COPY.md`, `02-HEROGALLERY-AUDIT.md`, `02-CASE-SOURCES.md`

    If any automated check fails, still write the file with the failure recorded, then stop and report
    before the human checkpoint — do not send Murilo to review a page that does not build or has a
    404ing asset.

    Leave the dev server running for Task 2 and note its URL in the file. Change no source file.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md && grep -q 'status: pending-human-review' .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md && test "$(grep -c 'SC-[1-5]' .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md || true)" -ge "5" && test -z "$(git status --porcelain src/)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run lint` shows no errors
    - `curl -s http://localhost:8080/ | grep -c "Resultado:"` returns 5
    - `curl -s http://localhost:8080/ | grep -ci "whatsapp\|wa.me"` returns 0
    - `curl -s http://localhost:8080/ | grep -c "hero-gallery-track"` returns 0
    - All 6 hero asset URLs and all 5 card image URLs return HTTP 200
    - All 10 route URLs listed above return HTTP 200 — zero broken links
    - `02-UAT.md` exists with `status: pending-human-review`, a populated `## Automated Pre-flight`
      table showing real captured output, and 5 empty-verdict `SC-` rows
    - `git status --porcelain src/` is empty — this task changes no source file
  </acceptance_criteria>
  <done>Every mechanical check that can be automated has been run and recorded, no asset or route 404s, and 02-UAT.md is staged with the 5 ROADMAP criteria awaiting a human verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking human review of the rebuilt home page against ROADMAP Phase 2 criteria</name>
  <action>
    Do not write, edit, or fix any file in this task. Present the review script below to the user
    against the dev server left running by Task 1, collect an explicit PASS/FAIL for each of SC-1
    through SC-5, and hold execution until the user responds. Record nothing here — Task 3 writes
    the verdict.
  </action>
  <what-built>
    The home page hero and case-studies section have been rebuilt on branch `feature/home-recruiter-redesign`:
    - Hero: full-colour responsive portrait of Murilo replaces the 9-image scrolling gallery; the H1
      now has two lines (role, then delivered result) using the copy approved in plan 02-01; the CTA
      row is now one filled "Conectar no LinkedIn" button plus outline "Enviar E-mail" and "Baixar CV"
      buttons, with "Ver Portfolio completo" demoted to an underlined text link below.
    - New "Resultados que já entreguei" section between the brand marquee and the dark Positioning
      band: 5 cards (NaTrave, Symplice, Maxi, Solid+, Kapyi), each showing Problema / Ação / Resultado
      as visible text.
    - The old hero gallery code was retired; all 9 of its images remain reachable via /trabalho
      (documented in 02-HEROGALLERY-AUDIT.md). No image file, stylesheet rule, or other page was changed.
    Automated pre-flight already passed: build, lint, all assets 200, all 10 routes 200, zero WhatsApp
    references, 5 "Resultado:" lines in the server HTML.
  </what-built>
  <how-to-verify>
    The dev server is running at http://localhost:8080/ — open it in a browser.

    1. **SC-1 — Hero scan (6-10 seconds).** Look at the hero without scrolling. Do you see a real
       photo of yourself (full colour, foreground — not a faded background layer)? Do you read a
       two-line headline where line 1 is a job title a recruiter would search for and line 2 states
       a result you delivered? Verdict: PASS / FAIL.
    2. **SC-2 — CTA hierarchy + squint test.** Still in the hero: are LinkedIn, E-mail and CV the
       first and heaviest things you want to click? Is "Ver Portfolio completo" clearly quieter?
       Now blur your eyes (or set browser zoom to 25%): does exactly ONE button style read as
       dominant? Confirm there is no WhatsApp button anywhere in the hero. Verdict: PASS / FAIL.
    3. **SC-3 — Case studies.** Scroll down to "Resultados que já entreguei". Count the cards — there
       must be 5: NaTrave, Symplice, Maxi, Solid+, Kapyi. For each, confirm you can read a Problema,
       an Ação and a Resultado without clicking or hovering. Then spot-check the claims: is every
       "Resultado" line something you actually delivered? Flag ANY sentence you would not say out
       loud to a recruiter — especially NaTrave's "2.250 seguidores" and Kapyi's qualitative line
       (Kapyi must have no invented number). Verdict: PASS / FAIL.
    4. **SC-4 — Nothing lost.** Click "Ver Portfolio completo" → /trabalho should load and still show
       every project, including Kmillion, Evidive, Talk2Buy and Milgrows, which are no longer on the
       home page. Click back and click into 2 of the 5 case cards — each should open its own project
       page normally. Verdict: PASS / FAIL.
    5. **SC-5 — No greeting, no new animation.** Confirm the hero contains no "Olá, sou o Murilo"-style
       greeting, and that nothing in the hero moves in a heavier way than the site did before (the
       fade-in and scroll-parallax that existed before are fine; a new video or a new motion sequence
       is not). Verdict: PASS / FAIL.
    6. **Mobile sanity (optional but recommended).** Resize the browser to ~375px wide. Is the headline
       readable and is the CTA cluster reachable without excessive scrolling? Note anything ugly —
       a formal mobile pass is Phase 4's job, but a blocker found now is cheaper.

    For any FAIL, say what you saw and which part is wrong (hero or cases). Do not fix anything yourself.
  </how-to-verify>
  <read_first>
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md` — the staged
      criteria table and the automated results to present alongside each criterion
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CASE-SOURCES.md` —
      have the provenance table on hand so any challenged claim can be traced to its source line live
  </read_first>
  <acceptance_criteria>
    - The user has given an explicit PASS or FAIL for each of SC-1 through SC-5 — no criterion is left
      unanswered or inferred from silence
    - Every FAIL is accompanied by a concrete observation (what was seen, where)
    - The user confirmed the squint test showed exactly one dominant CTA style (SC-2)
    - The user confirmed no case "Resultado" line makes a claim they would not stand behind (SC-3)
    - The user confirmed /trabalho still shows the non-featured projects (SC-4)
  </acceptance_criteria>
  <resume-signal>Reply with a verdict for each: `SC-1 PASS/FAIL`, `SC-2 PASS/FAIL`, `SC-3 PASS/FAIL`, `SC-4 PASS/FAIL`, `SC-5 PASS/FAIL` — adding a note for anything you marked FAIL. Or just say "tudo aprovado" if all five pass.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and route any gaps</name>
  <files>.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md</files>
  <read_first>
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md` — the staged
      table to fill in
    - `.planning/REQUIREMENTS.md` — the Traceability table rows for HERO-01..04, CASE-01..03,
      ANTI-02..05, to be flipped only if the phase passed
  </read_first>
  <action>
    Fill in the `## Success Criteria` table in `02-UAT.md` with the user's verdict and verbatim notes
    for each of SC-1 through SC-5. Update frontmatter: `status: complete`, and `verdict: pass` only if
    all five are PASS — otherwise `verdict: gaps-found`.

    If any criterion FAILED, add a `## Gaps` section with one row per failure: `gap id` |
    `criterion` | `what the user observed` | `owner:` (`02-02` for anything in the hero, `02-03` for
    anything in the case section) | `suggested fix`. Do not attempt the fix in this plan — gaps route
    to `/gsd:plan-phase 2 --gaps`. State that explicitly at the end of the section.

    If all five PASSED, add a `## Sign-off` section recording the date and the user's approval phrase,
    then update `.planning/REQUIREMENTS.md`'s Traceability table: flip HERO-01, HERO-02, HERO-03,
    HERO-04, CASE-01, CASE-02, CASE-03, ANTI-02, ANTI-03, ANTI-04 and ANTI-05 from `Pending` to
    `Complete`, and tick their checkboxes in the v1 Requirements list. Do not touch Phase 3's or
    Phase 4's rows.

    Stop the background dev server. Commit `02-UAT.md` (and `REQUIREMENTS.md` if it changed) on
    `feature/home-recruiter-redesign` with message `docs(02-04): record Phase 2 UAT verdict`.
    Never commit to `main`.

    Note for the next phase: the open OG-image gap flagged at the bottom of `.planning/ROADMAP.md`
    (a 1200x630 home-page Open Graph card, required by Phase 4's success criterion 4, currently owned
    by no phase) is still unassigned — record it as a carried-forward concern in the summary so
    Phase 3 planning can pick it up. Do not create the asset here; it is not in this phase's requirements.
  </action>
  <verify>
    <automated>grep -q 'status: complete' .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md && grep -Eq 'verdict: (pass|gaps-found)' .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md && test "$(grep -c 'SC-[1-5]' .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UAT.md || true)" -ge "5" && test "$(git rev-parse --abbrev-ref HEAD)" = "feature/home-recruiter-redesign"</automated>
  </verify>
  <acceptance_criteria>
    - `02-UAT.md` frontmatter reads `status: complete` and `verdict: pass` or `verdict: gaps-found`
    - All five SC rows have a non-empty human verdict cell
    - If `verdict: gaps-found`, a `## Gaps` section exists with an `owner:` value of `02-02` or `02-03`
      on every row, and the file names `/gsd:plan-phase 2 --gaps` as the next step
    - If `verdict: pass`, `.planning/REQUIREMENTS.md` shows all 11 Phase 2 requirement IDs as
      `Complete` in the Traceability table, and Phase 3/Phase 4 rows are unchanged
    - `git status --porcelain src/` is empty — this plan never modified source
    - `git log --oneline -1` shows the commit on `feature/home-recruiter-redesign`; `main` is unchanged
    - The background dev server process is no longer running on port 8080
  </acceptance_criteria>
  <done>The human verdict for all 5 ROADMAP criteria is recorded in 02-UAT.md, requirements are flipped to Complete on a pass or gaps are written up with an owning plan on a fail, and the record is committed on the feature branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev server → reviewer's browser | A background `vite dev` process is exposed on `host: "::"` port 8080 during the review window. |
| human verdict → requirement status | A verbal approval flips 11 requirement IDs to Complete, gating the milestone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | `vite dev` bound to `host: "::"` on port 8080 during review | accept | Pre-existing dev-server configuration in `vite.config.ts`, unchanged by this phase; the content served is a public marketing site with no secrets, no `.env`, and no auth. Task 3 stops the process when the review ends, limiting the window. |
| T-02-10 | Repudiation | Requirement completion driven by a verbal approval | mitigate | The verdict, per-criterion notes and approval phrase are written into `02-UAT.md` and committed to git before any requirement is flipped, producing an auditable trail (same pattern Phase 1's 01-03 checkpoint used). |
| T-02-11 | Elevation of Privilege | Accidental promotion of unreviewed work to the live site | mitigate | Every task asserts `feature/home-recruiter-redesign` as the branch and forbids commits to `main`; pushes to `main` auto-deploy to the live Lovable site, so the merge stays gated behind Phase 4. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this plan. `02-RESEARCH.md` "Package Legitimacy Audit" records zero external packages added for this phase. Nothing to audit. |
</threat_model>

<verification>
1. Automated pre-flight table in `02-UAT.md` contains real captured command output, not placeholders
2. All 5 SC rows carry an explicit human PASS/FAIL
3. On pass: REQUIREMENTS.md Traceability shows 11 Phase 2 IDs as Complete
4. On fail: every gap row names an owning plan (`02-02` or `02-03`) and the next command is stated
5. `git status --porcelain src/` empty across the whole plan
6. `git branch --show-current` is `feature/home-recruiter-redesign`; `main` untouched
</verification>

<success_criteria>
- ROADMAP Phase 2 success criteria 1-5 each have a recorded human verdict
- The squint test result (exactly one dominant CTA style) is explicitly confirmed, not assumed
- No route or asset returns anything but 200 — nothing was lost in the restructure
- Phase 2 either closes with all 11 requirement IDs Complete, or exits with a written, routable gap list
- Zero source files modified by this plan; zero commits on `main`
</success_criteria>

<output>
Create `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-04-SUMMARY.md` when done.
Record: the per-criterion verdicts, any gaps with their owning plan, the automated pre-flight results,
and the carried-forward OG-image concern (1200x630 home Open Graph card, still unassigned, needed by
Phase 4 success criterion 4) so Phase 3 planning picks it up.
</output>
