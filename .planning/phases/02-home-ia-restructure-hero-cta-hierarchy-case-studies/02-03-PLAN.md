---
phase: 02-home-ia-restructure-hero-cta-hierarchy-case-studies
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/routes/index.tsx
  - .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CASE-SOURCES.md
autonomous: true
requirements: [CASE-01, CASE-02, CASE-03, ANTI-03]

must_haves:
  truths:
    - "Scrolling past the hero, a recruiter sees exactly 5 case cards: NaTrave, Symplice, Maxi, Solid+, Kapyi — in that order."
    - "Each card shows a Problema / Ação / Resultado line as visible text — no hover, no click, no expand required to read the outcome."
    - "Every sentence in every card traces to copy already published on that project's own route file — no invented metric anywhere, and no number at all on the Kapyi result line."
    - "The three projects dropped from the home array (Kmillion, Evidive, Talk2Buy) are still listed on /trabalho, and the home page still links to /trabalho."
    - "The home page shows 5 cases, not the full project list."
  artifacts:
    - path: "src/routes/index.tsx"
      provides: "5-entry projects array with problem/action/result fields, P→A→R figcaption, and the rendered Case Studies section"
      contains: "Resultados que já entreguei"
    - path: ".planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CASE-SOURCES.md"
      provides: "Line-by-line provenance table mapping every P/A/R sentence to its source route file and line, plus the CASE-03 reachability check for the 3 dropped projects"
      contains: "source line"
  key_links:
    - from: "src/routes/index.tsx projects array"
      to: "src/routes/{natrave,symplice,maxi,solid,kapyi}.tsx"
      via: "P→A→R copy condensed from each project's BrandHeader description / PerformanceHero followers"
      pattern: "to: \"/(natrave|symplice|maxi|solid|kapyi)\""
    - from: "src/routes/index.tsx Case Studies section"
      to: "ProjectCard"
      via: "projects.map rendering 5 cards in array order"
      pattern: "projects\\.map"
    - from: "src/routes/index.tsx"
      to: "/trabalho"
      via: "hero 'Ver Portfolio completo' text-link (shipped in 02-02) keeps the full list reachable"
      pattern: "Ver Portfolio completo"
---

## Phase Goal

Source: `.planning/ROADMAP.md` Phase 2 `**Goal:**` line, quoted verbatim (the ROADMAP goal for this
phase is not written in canonical `As a / I want to / so that` syntax — it is reproduced as-is rather
than invented into that shape):

> A recruiter landing on the home page immediately sees who Murilo is (role + top result), a visually
> unambiguous CTA hierarchy favoring LinkedIn/e-mail/CV, and real proof of delivered work via 5 featured
> case studies — without losing access to any other project.

## Slice Outcome (this plan)

After this plan, a recruiter who scrolls past the (already working) hero reads 5 real case studies with
a visible Problema → Ação → Resultado narrative and can click any of them into its full project page.
This completes the second half of the phase's vertical slice — the home page now delivers both identity
and proof.

<objective>
Activate the dead `projects` array and `ProjectCard` component in `src/routes/index.tsx`: redefine the
array to the 5 D-01-confirmed cases with `problem`/`action`/`result` fields sourced verbatim-condensed
from each project's own route file, restructure `ProjectCard`'s `<figcaption>` into a visible P→A→R
definition list, and render the whole thing as a new "Case Studies" section between Brand Marquee and
Positioning.

Purpose: CASE-01/02 are the proof half of Phase 2's goal — a recruiter must see delivered outcomes
without clicking. `ARCHITECTURE.md` names this the single highest-leverage build task on the page.
CASE-03 and ANTI-03 constrain it: exactly 5 on the home, everything else stays reachable via `/trabalho`.
Output: An activated case-studies section plus `02-CASE-SOURCES.md` (copy provenance + reachability proof).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CONTEXT.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md
@.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-PATTERNS.md
@src/routes/index.tsx
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase on 2026-08-05. No exploration required. -->

Current `projects` array entry shape (`src/routes/index.tsx` line 41, 4 fields — extend, do not replace
the shape): `{ name, category, image, to }`. Target shape adds three string fields in this exact
declaration order after `to`: `problem`, `action`, `result`. The field order matters — plan
verification greps `to: "/kapyi"` plus the following lines.

`ProjectCard` signature (`src/routes/index.tsx` line 199, unchanged by this plan):
`function ProjectCard({ project, index }: { project: any, index: number })`
Internals that must stay untouched: the `isVisible` state, the `cardRef` `IntersectionObserver`
callback, `<Link to={project.to} className="group">`, `<figure ref={cardRef} className="scroll-reveal
project-card relative" style={{ transitionDelay: `${index * 100}ms` }}>`, and the
`media-wrap aspect-[4/3] rounded-2xl overflow-hidden border border-border/5` wrapper around
`<ProjectMedia>`. Only the `<figcaption>` internals (lines 231–237) change.

Verified source strings — these already exist on disk, grep them to confirm before condensing:
| Case | Source file:line | Verbatim source |
|------|------------------|-----------------|
| NaTrave | `src/routes/natrave.tsx:69` | `followers={2250}` (the ONLY real numeric metric among the 5) |
| Symplice | `src/routes/symplice.tsx:25` | "Para marcas que operam na complexidade, a Symplice entrega simplicidade estratégica. Uma identidade visual limpa, direta e focada na experiência do usuário." |
| Maxi | `src/routes/maxi.tsx:44` | "O Colégio Maxi é uma instituição de ensino com trajetória consolidada, focada em excelência acadêmica, desenvolvimento humano e aprovações em vestibulares." |
| Solid+ | `src/routes/solid.tsx:25` | "Fintech Identity & Systems. Design que transmite solidez, confiança e inovação para o mercado financeiro digital." |
| Kapyi | `src/routes/kapyi.tsx:43` | "Fugindo de fórmulas prontas. Uma atuação de 2,5 anos unindo branding e direção criativa para posicionar marcas de forma premium no ambiente online." |

Card image paths, verified present on disk with sizes:
`/assets/projects/thumbnails/natrave.jpg` (2.2MB) · `/assets/projects/thumbnails/symplice.jpg` (1.8MB) ·
`/assets/projects/thumbnails/social/maxi.jpg` (2.6MB) · `/assets/projects/thumbnails/solid.jpg` (1.9MB) ·
`/assets/projects/thumbnails/social/kapyi.jpg` (0.83MB).
There is NO `thumbnails/maxi.jpg` — Maxi lives under `thumbnails/social/`. Never use
`solid-full.png` (8.2MB, 02-RESEARCH.md Pitfall 2).

Section-wrapper convention to copy (`src/routes/index.tsx` line 334, the Methods section):
`<section className="site-section border-t border-border relative z-10 bg-background">` →
`<div className="site-container">` → `<div className="mb-16">` → `scroll-reveal` heading.
`.tag-mono` is declared in `src/styles.css` line 252 and is the eyebrow class.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Source the case copy and redefine the projects array to the confirmed 5</name>
  <files>src/routes/index.tsx, .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CASE-SOURCES.md</files>
  <read_first>
    - `src/routes/natrave.tsx` (around line 52–83, `BrandHeader` + `PerformanceHero followers={2250}`)
    - `src/routes/symplice.tsx` (around line 24–26, `BrandHeader`)
    - `src/routes/maxi.tsx` (around line 43–45, `BrandHeader`)
    - `src/routes/solid.tsx` (around line 24–26, `BrandHeader`)
    - `src/routes/kapyi.tsx` (around line 31, 42–44, 65 — `BrandHeader` + `AbordagemEstrategica` stats)
    - `src/routes/index.tsx` lines 41–78 — the current 6-entry `projects` array being redefined
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-PATTERNS.md` lines
      167–241 — the target array, written out entry by entry
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CONTEXT.md` — D-01
      (Kapyi is the 5th case, not Milgrows) and D-02 (Kapyi qualitative only, never invent a number)
  </read_first>
  <action>
    Read all five project route files first and confirm the source strings listed in `<interfaces>`
    above still match the files on disk. If any string has drifted, use the on-disk version and note
    the drift in the sources file — never fall back to the version quoted in a planning document.

    Then replace the `projects` array in `src/routes/index.tsx` (lines 41–78) with exactly five
    entries, in this order: NaTrave, Symplice, Maxi, Solid+, Kapyi (per D-01). Each entry declares
    its fields in this order: `name`, `category`, `image`, `to`, `problem`, `action`, `result`.
    Use the exact values written out in 02-PATTERNS.md lines 186–239 — including the `image` paths
    (`social/maxi.jpg` and `social/kapyi.jpg` are under the `social/` subdirectory; Solid+ uses
    `thumbnails/solid.jpg`, never `solid-full.png`).

    Anti-fabrication rules, non-negotiable (02-RESEARCH.md Pitfall 4, D-02):
    - NaTrave's `result` is the only line that may contain a number, and that number must be 2.250
      (traceable to `followers={2250}` in `natrave.tsx`).
    - Symplice, Maxi, Solid+ and Kapyi `result` lines are qualitative — a qualitative result is the
      CORRECT final state for these four, not a placeholder awaiting a metric.
    - Kapyi's `result` line must contain no digit at all. The "2,5 anos" figure may appear only in
      Kapyi's `action` line, where it is verbatim-sourced from `kapyi.tsx` line 43.
    - Each of `problem`/`action`/`result` is one short sentence. Do not compress a whole project page
      into a card (`ARCHITECTURE.md` Anti-Pattern 1: the card justifies a click, it does not replace it).

    Kmillion, Evidive and Talk2Buy leave this array only. They keep their own routes and remain listed
    on `/trabalho`. Do not touch `src/routes/trabalho.tsx`, and do not delete any project route file.

    Create `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CASE-SOURCES.md`
    with YAML frontmatter (`phase: 02`, `plan: 03`, `constraint: no-fabricated-metrics`) and a table
    of 15 rows — one per P/A/R field across the 5 cases — with columns `Case` | `Field` | `Shipped
    sentence` | `Source file:line` | `Grep used`. Add a `## CASE-03 Reachability` section with a
    3-row table for Kmillion, Evidive and Talk2Buy showing the `/trabalho` line that still lists each
    one, plus their still-live route files.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test "$(grep -c 'to: "/' src/routes/index.tsx || true)" = "5" && grep -q 'followers={2250}' src/routes/natrave.tsx && grep -q 'simplicidade estratégica' src/routes/symplice.tsx && grep -q 'excelência acadêmica' src/routes/maxi.tsx && grep -q 'mercado financeiro digital' src/routes/solid.tsx && grep -q '2,5 anos' src/routes/kapyi.tsx && test "$(grep -A4 'to: "/kapyi"' src/routes/index.tsx | grep 'result:' | grep -c '[0-9]' || true)" = "0" && test "$(grep -c 'solid-full.png' src/routes/index.tsx || true)" = "0" && test -f .planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-CASE-SOURCES.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - The `projects` array has exactly 5 entries; `grep -c 'to: "/' src/routes/index.tsx` returns 5
      and those five values are `/natrave`, `/symplice`, `/maxi`, `/solid`, `/kapyi` in that order
    - Every entry has all seven fields (`name`, `category`, `image`, `to`, `problem`, `action`, `result`)
    - `grep -A4 'to: "/kapyi"' src/routes/index.tsx | grep 'result:' | grep -c '[0-9]'` returns 0 (D-02)
    - Only NaTrave's `result:` line contains a digit, and that digit sequence is `2.250`
    - `grep -c "solid-full.png" src/routes/index.tsx` returns 0 and `grep -c "milgrows" src/routes/index.tsx` returns 0
    - All 5 `image:` paths resolve to real files: `ls public<path>` succeeds for each
    - `02-CASE-SOURCES.md` has 15 P/A/R provenance rows and a 3-row CASE-03 reachability table
    - Every sentence in the provenance table has a non-empty `Source file:line` that returns a grep hit
    - `git status --porcelain src/routes/trabalho.tsx` is empty; no project route file was deleted
  </acceptance_criteria>
  <done>The projects array holds the 5 confirmed cases with sourced P/A/R copy, Kapyi carries no fabricated number, and 02-CASE-SOURCES.md documents where every sentence came from and where the 3 dropped projects still live.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure ProjectCard's figcaption into a visible P→A→R block</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - `src/routes/index.tsx` lines 199–241 — the full `ProjectCard` component. Only lines 231–237
      (`<figcaption>`) change; everything else is load-bearing and stays.
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-PATTERNS.md` lines
      249–279 — current vs. target `<figcaption>` JSX, given verbatim, plus the note that the existing
      `font-bold text-lg` / `.project-card figcaption` dual declaration is grandfathered and must not
      be "reconciled".
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md` —
      Typography table (P→A→R body at 14px / `text-sm`, labels bold) and Color table (P→A→R `<dt>`
      labels use `text-foreground`, `<dd>` sentences use `text-secondary`)
  </read_first>
  <action>
    Replace only the `<figcaption>` block inside `ProjectCard` (lines 231–237) with the target
    structure from 02-PATTERNS.md lines 261–275:

    Outer `<figcaption className="mt-6">` wrapping two children.
    Child 1: `<div className="flex justify-between items-start">` preserving today's inner content —
    the `<span className="text-[10px] font-mono uppercase tracking-widest text-secondary mb-1 block">`
    category, the `<span className="font-bold text-lg leading-tight block tracking-tight mb-3">` name
    (note the added `mb-3` to separate the title from the new list), and the existing
    `<ArrowRight size={18} className="opacity-0 group-hover:opacity-100 -translate-x-2
    group-hover:translate-x-0 transition-all" />` hover affordance.
    Child 2: `<dl className="text-sm text-secondary space-y-1">` with three `<div>` rows, each holding
    an inline `<dt className="inline font-bold text-foreground">` label and an inline
    `<dd className="inline">` value: `Problema: ` / `{project.problem}`, `Ação: ` / `{project.action}`,
    `Resultado: ` / `{project.result}`. Label text is locked by the UI-SPEC Copywriting Contract —
    exactly "Problema:", "Ação:", "Resultado:", each with a trailing space before the value.

    CASE-02 is satisfied structurally: the `<dl>` is plain rendered text with no `hidden`,
    `group-hover:`, `max-h-0`, `sr-only`, `<details>`, or click-to-expand wrapper. The result must be
    readable in the static SSR'd HTML.

    Do not change the component signature, the `isVisible`/`cardRef` `IntersectionObserver` wiring,
    the `<Link>`, the `<figure>` classes, the `transitionDelay` style, the `media-wrap aspect-[4/3]`
    wrapper, or the `<ProjectMedia>` call — 02-RESEARCH.md explicitly names "rewriting ProjectCard
    from scratch" as an anti-pattern. Do not add `loading="eager"` or `fetchPriority` to card images;
    they keep `ProjectMedia`'s `loading="lazy"` default (02-RESEARCH.md Pitfall 2 — five ~2MB
    thumbnails must not compete with the hero portrait for first paint).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'Problema:' src/routes/index.tsx && grep -q 'Ação:' src/routes/index.tsx && grep -q 'Resultado:' src/routes/index.tsx && grep -q '{project.problem}' src/routes/index.tsx && grep -q '{project.action}' src/routes/index.tsx && grep -q '{project.result}' src/routes/index.tsx && test "$(grep -c 'IntersectionObserver' src/routes/index.tsx || true)" = "1" && test "$(grep -c 'aspect-\[4/3\]' src/routes/index.tsx || true)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `ProjectCard` still contains `IntersectionObserver`, `scroll-reveal project-card relative`,
      `transitionDelay`, `media-wrap aspect-[4/3] rounded-2xl overflow-hidden border border-border/5`,
      and the `<ProjectMedia>` call — `git diff src/routes/index.tsx` shows changes confined to the
      `<figcaption>` block
    - The `<figcaption>` contains a `<dl>` with exactly three `<dt>`/`<dd>` pairs
    - The three `<dt>` labels read exactly `Problema: `, `Ação: `, `Resultado: `
    - `grep -c "hidden\|sr-only\|<details>" ` inside the figcaption block returns 0 — the P→A→R text is
      unconditionally rendered (CASE-02)
    - No `loading="eager"` or `fetchPriority` appears anywhere inside `ProjectCard`
    - `npm run lint` reports no new errors versus the pre-task baseline
  </acceptance_criteria>
  <done>ProjectCard renders category, name, hover arrow, and a three-row Problema/Ação/Resultado definition list as always-visible text, with all its existing reveal and media wiring intact.</done>
</task>

<task type="auto">
  <name>Task 3: Render the Case Studies section and verify /trabalho reachability</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - `src/routes/index.tsx` lines 307–331 — the Brand Marquee section (ends line 317) and the
      Positioning section (starts line 319); the new section is inserted between them.
    - `src/routes/index.tsx` line 334 — the Methods section wrapper, the exact background/border
      convention to copy (`site-section border-t border-border relative z-10 bg-background`).
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-PATTERNS.md` lines
      283–329 — target section JSX, verbatim.
    - `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-UI-SPEC.md` §4 —
      grid decision (`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-8 gap-y-16`, natural flow, no
      special treatment for the 5th card) and the locked eyebrow/heading copy.
    - `src/styles.css` line 252 — the `.tag-mono` class definition.
  </read_first>
  <action>
    Insert a new `{/* Case Studies */}` section into `HomePage`'s JSX, positioned after the Brand
    Marquee `</section>` (line 317) and before the `{/* Positioning */}` comment (line 319).

    Structure, per 02-PATTERNS.md lines 312–327: `<section className="site-section border-t
    border-border relative z-10 bg-background">` → `<div className="site-container">` →
    `<div className="mb-16">` holding a `<span className="tag-mono scroll-reveal mb-4 inline-block">`
    eyebrow with the locked text `Prova de Trabalho` and an `<h2 className="text-3xl md:text-5xl
    lg:text-6xl font-bold tracking-tighter leading-[1.1] max-w-4xl scroll-reveal">` with the locked
    text `Resultados que já entreguei`. Below the heading block, a
    `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-8 gap-y-16">` rendering
    `projects.map((project, i) => <ProjectCard key={project.to} project={project} index={i} />)`.

    Constraints:
    - Do NOT re-sort, filter, or slice inside the `.map()` — card order comes from the array order
      set in Task 1 (NaTrave, Symplice, Maxi, Solid+, Kapyi).
    - The section stays on the default light `bg-background` — do NOT apply the Positioning section's
      `bg-foreground text-background` inversion (UI-SPEC §1).
    - Do NOT add a new `IntersectionObserver` or a Framer Motion `whileInView` sequence. Reveal comes
      from the `scroll-reveal` class plus the `useScrollReveal` hook already wired at `HomePage` line
      244 (ANTI-05).
    - Exactly 5 cards render (ANTI-03) — the home page must not become a second `/trabalho`.

    Then verify CASE-03 without editing anything: confirm `src/routes/trabalho.tsx` still lists
    Kmillion, Evidive and Talk2Buy, and confirm the hero's `Ver Portfolio completo` link to
    `/trabalho` (shipped by plan 02-02) is still present in `index.tsx`. Append the results as a
    `## Post-render verification` section in `02-CASE-SOURCES.md`.

    Commit on `feature/home-recruiter-redesign` with message
    `feat(02-03): activate 5 featured case studies with Problema/Ação/Resultado narrative`.
    Never commit to `main`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'Resultados que já entreguei' src/routes/index.tsx && grep -q 'Prova de Trabalho' src/routes/index.tsx && grep -q 'md:grid-cols-2 lg:grid-cols-3 gap-x-8 gap-y-16' src/routes/index.tsx && test "$(grep -c 'projects.map' src/routes/index.tsx || true)" = "1" && test "$(grep -c 'bg-foreground text-background' src/routes/index.tsx || true)" = "1" && grep -q 'Ver Portfolio completo' src/routes/index.tsx && grep -q '"/kmillion"' src/routes/trabalho.tsx && grep -q '"/evidive"' src/routes/trabalho.tsx && grep -q '"/talk2buy"' src/routes/trabalho.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run lint` reports no new errors
    - The Case Studies `<section>` appears between the Brand Marquee `</section>` and the
      `{/* Positioning */}` comment in `src/routes/index.tsx`
    - `grep -c "projects.map" src/routes/index.tsx` returns 1, with no `.slice(`, `.filter(`, or
      `.sort(` applied to `projects` anywhere in the file
    - `grep -c "bg-foreground text-background" src/routes/index.tsx` returns 1 (only the pre-existing
      Positioning section is inverted — the new section is not)
    - With `npm run dev` running, `curl -s http://localhost:8080/ | grep -c "Resultado:"` returns 5
      (CASE-02 — outcomes present in the SSR'd HTML, no click required)
    - `curl -s http://localhost:8080/ | grep -c 'href="/natrave"'` returns at least 1, and the same
      holds for `/symplice`, `/maxi`, `/solid`, `/kapyi`
    - `src/routes/trabalho.tsx` still contains `/kmillion`, `/evidive` and `/talk2buy` and is
      unmodified (`git status --porcelain src/routes/trabalho.tsx` empty) — CASE-03
    - `02-CASE-SOURCES.md` has a `## Post-render verification` section recording the curl counts
    - `git log --oneline -1` shows the commit on `feature/home-recruiter-redesign`; `main` unchanged
  </acceptance_criteria>
  <done>The home page renders a Case Studies section of exactly 5 cards between the marquee and the Positioning band, each showing its P→A→R text in the server-rendered HTML, with /trabalho still holding the full list and still linked from the hero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project route files → home page card copy | Published case copy is re-authored into a condensed form on a higher-traffic page; misstatement here is a credibility/misrepresentation risk, not a code-execution one. |
| public web → SSR'd case markup | All 5 card entries are hardcoded module-level constants — no user input, no data fetch, no query param reaches this section. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Repudiation | Case `result` claims shown to recruiters | mitigate | Every P/A/R sentence must have a `Source file:line` row in `02-CASE-SOURCES.md` backed by a real grep hit. Kapyi's result line is digit-free by acceptance criterion (D-02). NaTrave's 2.250 traces to `natrave.tsx:69`. A claim with no source row fails the task. |
| T-02-07 | Information Disclosure | Client-identifying copy on the home page | accept | All 5 client names and their descriptions are already publicly published on this same site's project routes; condensing them onto the home page discloses nothing new. |
| T-02-05b | Denial of Service | Five ~2MB thumbnails below the fold | mitigate | Card images stay on `ProjectMedia`'s `loading="lazy"` default; `loading="eager"`/`fetchPriority="high"` remain exclusive to the hero portrait. Asserted in Task 2 acceptance criteria. Any residual page-weight finding routes to Phase 4 / tech debt, not to a new dependency here. |
| T-02-08 | Tampering | Card copy rendered into the DOM | mitigate | All strings are module-level constants rendered as JSX text children (React auto-escapes); `dangerouslySetInnerHTML` is not used and must stay at 0 occurrences. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this plan. `02-RESEARCH.md` "Package Legitimacy Audit" records zero external packages added for this phase. Nothing to audit. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0; `npm run lint` shows no new errors
2. `npm run dev` boots on port 8080; `curl -s http://localhost:8080/ | grep -c "Resultado:"` returns 5
3. `grep -c 'to: "/' src/routes/index.tsx` returns 5 (ANTI-03 — home shows 5, not the full list)
4. `grep -A4 'to: "/kapyi"' src/routes/index.tsx | grep 'result:' | grep -c '[0-9]'` returns 0 (D-02)
5. Every P/A/R sentence has a verifiable `Source file:line` row in `02-CASE-SOURCES.md`
6. `src/routes/trabalho.tsx` is unmodified and still lists Kmillion, Evidive, Talk2Buy (CASE-03)
7. `git status --porcelain public/` and `git diff --stat src/styles.css` are both empty
8. `git branch --show-current` is `feature/home-recruiter-redesign`; no commit reached `main`
</verification>

<success_criteria>
- CASE-01: 5 case cards render on the home page — NaTrave, Symplice, Maxi, Solid+, Kapyi — each with a Problema → Ação → Resultado narrative
- CASE-02: every card's outcome is readable in the server-rendered HTML with no click, hover, or expand
- CASE-03: Kmillion, Evidive, Talk2Buy (and every other project) remain listed on `/trabalho`, which is still linked from the hero
- ANTI-03: the home page shows exactly 5 cases, not the full project list
- D-01 honoured: the 5th case is Kapyi, not Milgrows
- D-02 honoured: Kapyi's result line is qualitative and contains no number; no metric is fabricated for any case
- Zero image assets, stylesheet rules, or other route files were modified
</success_criteria>

<output>
Create `.planning/phases/02-home-ia-restructure-hero-cta-hierarchy-case-studies/02-03-SUMMARY.md` when done.
Record: the 5 shipped case entries with their exact P/A/R sentences, the curl-verified `Resultado:`
count, confirmation that `/trabalho` and `src/styles.css` were untouched, and the path to
`02-CASE-SOURCES.md` so plan `02-04`'s human reviewer can spot-check any claim against its source.
</output>
